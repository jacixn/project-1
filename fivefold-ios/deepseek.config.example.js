// Copy to deepseek.config.js (gitignored) and fill in your own values.
// Keys are Biblely's own; never reuse another app's.
// Deepseek API config with primary + fallback key.
// Primary is the 'YOUR_VALUE_HERE' key; fallback is the original Deepseek key.

export const DEEPSEEK_CONFIG = {
  apiUrl: 'YOUR_VALUE_HERE',
  // Primary key (used first)
  apiKey: 'YOUR_VALUE_HERE',
  // Fallback key (used if 401/unauthorized occurs)
  fallbackApiKey: 'YOUR_VALUE_HERE',
};


